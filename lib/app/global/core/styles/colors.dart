import 'dart:ui';

class S2BColors {
  S2BColors._();

  /// primaryColor = #0A3987
  static const primaryColor = Color(0xffB60303);

  static const secondaryColor = Color(0xFF900303);

  /// disabledColor = #C6C6C6
  static const disabledColor = Color(0xFFC6C6C6);

  /// white = #FFFFFF
  static const white = Color(0xFFFFFFFF);
  static const background = Color(0xFFEBEFFB);

  ///whiteSecundary  = #dddddd
  static const Color whiteSecundary = Color(0xffdddddd);

  ///graySecondary  = #e5e5e5
  static const Color graySecondary = Color(0xFFC3C3C3);

  ///disabledButtonColor  = #efefef
  static const Color disabledButtonColor = Color(0xffefefef);

  ///silver = #aaaaaa
  static const Color silver = Color(0xffaaaaaa);

  ///silver = #EB2A1C
  static const Color dangerColor = Color(0xFFEB2A1C);

  /// deepSaffron = #F59535
  static const Color orange = Color(0xffF59535);

  /// deepSaffron = #F59535
  static const Color green = Color(0xFF138D75);

  /// deepSaffron = #F59535
  static const Color yellow = Color(0xFFf4ba1a);

  /// black = #000000
  static const black = Color(0xF0000000);

  /// black = #2196F3
  static const blue = Color(0xFF2196F3);

  static const colorError = Color(0xFFC02929);
  static const colorHover = Color(0xFF0B0E14);
  static const colorFocused = Color(0xFF545454);
  static const colorInactive = Color(0xFFB6B6B6);
  static const colorSuccess = Color(0xff0A3987);

  /// borderColor of [Toast] when [ToastType.success]
  static const msgSuccessBR = Color(0xFF138D75);

  /// backgroundColor of [Toast] when [ToastType.success]
  static const msgSuccessBG = Color(0xFFE8F6F3);

  /// textColor of [Toast] when [ToastType.success]
  static const msgSuccessTX = Color(0xFF0B4A3E);

  /// borderColor of [Toast] when [ToastType.info]
  static const msgInfoBR = Color(0xff0A3987);

  /// backgroundColor of [Toast] when [ToastType.info]
  static const msgInfoBG = Color(0xFFE5F6FD);

  /// textColor of [Toast] when [ToastType.info]
  static const msgInfoTX = Color(0xFF0C2D60);

  /// borderColor of [Toast] when [ToastType.warning]
  static const msgWarningBR = Color(0xFF1565C0);

  /// backgroundColor of [Toast] when [ToastType.warning]
  static const msgWarningBG = Color(0xFFFFF3E0);

  /// textColor of [Toast] when [ToastType.warning]
  static const msgWarningTX = Color(0xFF8B480D);

  /// borderColor of [Toast] when [ToastType.error]
  static const msgErrorBR = Color(0xFFC62828);

  /// backgroundColor of [Toast] when [ToastType.error]
  static const msgErrorBG = Color(0xFFFFEBEE);

  /// textColor of [Toast] when [ToastType.error]
  static const msgErrorTX = Color(0xFFC62828);
}
